---
spring:
  cloud:
    config:
      client:
        oauth2:
          client-id: "<client id>"
          client-secret: "<client secret>"
          access-token-uri: "https://acccess/token/url"
cook:
  special: Frog Legs
secretMenu: Tofu Surprise
